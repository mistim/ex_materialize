defmodule Mix.Tasks.Materialize.InstallTest do
  use ExUnit.Case
  doctest Mix.Tasks.Materialize.Install

#  test "get Phoenix version" do
#    IO.inspect(Mix.Tasks.Materialize.Install.get_project_dep :phoenix)
#    assert is_binary Mix.Tasks.Materialize.Install.get_project_dep :phoenix
#  end

#  test "check Phoenix version success" do
#    assert Mix.Tasks.Materialize.Install.check_version(:phoenix, "1.3.0")
#  end
#
#  test "check Phoenix version wrong" do
#    assert 1 + 1 == 2
#  end
end